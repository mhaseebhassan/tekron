option_settings:
  aws:elasticbeanstalk:application:environment:
    NODE_ENV: production

container_commands:
  01_prisma_generate:
    command: "npx prisma generate"
  02_next_build:
    command: "npm run build"
